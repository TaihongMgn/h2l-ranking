services:
  h2l-ranking:
    image: nginx:alpine
    container_name: h2l-ranking
    restart: unless-stopped
    ports:
      - "127.0.0.1:8090:80"
    volumes:
      - ./www:/usr/share/nginx/html:ro
    cpus: 0.5
    mem_limit: 256m
